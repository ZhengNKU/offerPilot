fastapi
uvicorn[standard]
sqlalchemy>=2.0.0
asyncpg
redis
pyjwt
bcrypt
tencentcloud-sdk-python
pydantic>=2.0.0
pydantic-settings
email-validator
python-dotenv
python-multipart
cos-python-sdk-v5

# DOCX resume rewrite (preserve original style, only replace bullet text)
python-docx>=1.0
# PDF reading / resume parsing
pypdf>=4.0

# PDF -> DOCX conversion (PyMuPDF backend, no LibreOffice needed)
pdf2docx>=0.5

# 实时语音面试（Volcano Doubao Realtime WSS client）
websockets>=12.0

# AI 职业顾问：阿里百炼 Qwen Embedding 异步 HTTP 客户端
httpx>=0.27
# pgvector Python 类型映射（Vector(N) 在 SQLAlchemy 中使用）
pgvector>=0.3

# 阿里百炼 WebSearch MCP 客户端（streamable_http + ClientSession）
# 必须 >=1.10：streamable_http_client 需要返回 3 元组 + 支持 terminate_on_close
# 锁版本与本地一致，避免 SDK 升级/降级导致 API 不兼容（ValueError: not enough values to unpack）
mcp==1.27.0

# 阿里百炼 ASR SDK（2026-08-02+ 切换 qwen-audio-3.0-asr-flash-filetrans）
# 提供 Transcription.async_call + Transcription.wait 用于长音频文件转写
dashscope>=1.20.0
