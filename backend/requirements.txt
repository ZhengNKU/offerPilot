fastapi
uvicorn[standard]
sqlalchemy>=2.0.0
asyncpg
redis
pyjwt
bcrypt
tencentcloud-sdk-python
pydantic>=2.0.0
pydantic-settings
email-validator
python-dotenv
python-multipart
cos-python-sdk-v5

# DOCX resume rewrite (preserve original style, only replace bullet text)
python-docx>=1.0
# PDF reading / resume parsing
pypdf>=4.0

# PDF -> DOCX conversion (PyMuPDF backend, no LibreOffice needed)
pdf2docx>=0.5

# 实时语音面试（Volcano Doubao Realtime WSS client）
websockets>=12.0

# AI 职业顾问：阿里百炼 Qwen Embedding 异步 HTTP 客户端
httpx>=0.27
# pgvector Python 类型映射（Vector(N) 在 SQLAlchemy 中使用）
pgvector>=0.3
# 腾讯云 WebSearch MCP 客户端（官方 SDK：sse_client + ClientSession）
mcp>=1.0
# MCP SDK 1.28.x 在 import mcp.server.lowlevel 时会直接 import jsonschema
jsonschema>=4.23


